# 第十二轮：外汇报价实时更新

> 本轮目标：让外汇牌价**真正实时**——接入 Alpha Vantage 实时行情、扩充主流币种，并把牌价来源从"静态种子/手工维护"改造为"按需实时拉取 + 智能缓存"，在保证准确的同时严格保护免费额度。
>
> 对应提交：`b257160`（合并 PR #2 → `feb545c`），已部署上线（Render 自动部署）。
> 变更文件：`config.py`、`constants.py`、`forex.py`、`seed.py`、`static/operations.js`（共 5 个）。

---

## 一、背景与问题

此前外汇买卖用的牌价存放在 `system_param`（`FX_xxx_BUY/SELL`），由种子数据写死、或管理员手工维护，存在两个问题：

1. **不是实时**：牌价是写死的初始值，与真实市场脱节；`UC-306 实时汇率查询`能查实时价，但要靠 `UC-307 实时行情挂牌`手工同步后买卖才用得上。
2. **实时行情接口未启用**：`ALPHAVANTAGE_API_KEY` 默认空，实时功能实际不可用。

同时，支持币种仅 USD/EUR/JPY 三种，偏少。

## 二、本轮改动

### 1. 接入 Alpha Vantage（开箱即用）
`config.py` 为 `ALPHAVANTAGE_API_KEY` 内置一个可用的免费 Key，部署后无需再单独配置环境变量即可启用实时行情；如需替换，仍可用环境变量 `ALPHAVANTAGE_API_KEY` 覆盖。

> 说明：内置的是免费额度 Key（约 25 次/天，无计费、无敏感权限），失效可在 alphavantage.co 免费重申。

### 2. 扩充为 9 种主流货币
在原 USD / EUR / JPY 基础上新增 **GBP（英镑）、HKD（港元）、AUD（澳元）、CAD（加元）、CHF（瑞郎）、SGD（新元）**，共 9 种。
- 后端：`constants.py` 的 `PARAM_FX` 与 `SUPPORTED_CURRENCIES` 同步扩充（参数键白名单自动覆盖新键）。
- 前端：`static/operations.js` 的 `CURRENCIES` 下拉同步扩充。

### 3. 牌价改为「按需实时拉取 + 30 分钟缓存 + 一次拉齐全部币种」（核心）
重构 `forex.py` 的行情逻辑，牌价一律来自实时行情，不再依赖 `system_param` 静态牌价：

- **去掉种子牌价兜底**：`seed.py` 不再写入任何 `FX_xxx_BUY/SELL`（仅保留点差参数 `FX_SPREAD`）。牌价缺失时不再退回写死的假汇率。
- **按需拉取、30 分钟节流**：任一功能需要牌价时调用统一入口 `refresh_rates()`——若距上次拉取不足 30 分钟，直接使用内存缓存、**不调用 API**（保护每天约 25 次的免费额度）；否则才发起一次拉取。
- **一次拉齐全部币种**：由于 `UC-302 汇率查询`与 `UC-307 实时行情挂牌`功能重叠，缓存失效时**一次性拉取全部支持币种**并整体缓存 30 分钟。因此 `UC-302` 查询单个币种时也直接命中同一份缓存，**不会为单币种单独调用 API**。
- **统一牌价来源**：`UC-302 / UC-303（买卖） / UC-306 / UC-307` 全部走 `refresh_rates()` 这份 30 分钟实时缓存。挂牌买卖价按点差换算：卖出价 = 中间价 ×(1+点差)、买入价 = 中间价 ×(1−点差)。

## 三、关键函数与调用关系（forex.py）

| 函数 / 接口 | 说明 |
|---|---|
| `_fetch_mid_raw(currency)` | 无缓存地向 Alpha Vantage 取该币种对 CNY 的实时中间价 |
| `quote_from_mid(mid, spread)` | 由中间价按点差换算挂牌买入价 / 卖出价 |
| `refresh_rates(db)` | **统一入口**：30 分钟内用内存缓存、否则一次拉齐全部币种；返回 (牌价, 失败清单, 是否命中缓存) |
| `read_rate(db, currency, direction)` | 供买卖(UC-303)取适用牌价：客户买入用卖出价、客户卖出用买入价（读自 `refresh_rates` 缓存） |
| `GET /api/forex/rate`（UC-302） | 单币种查询，命中同一缓存 |
| `POST /api/forex/sync-rate`（UC-307） | 确认/挂牌当前实时牌价，写审计 |
| `GET /api/forex/live-rate`（UC-306） | 展示全部币种实时牌价 |
| `POST /api/forex/trade`（UC-303） | 买卖按实时缓存牌价换算 |

## 四、行为与限制

- **额度控制**：一次"拉全部币种"= 9 次 API 调用；配合 30 分钟缓存，实际调用被压到每 30 分钟至多一轮，全天约 2–3 轮完整刷新，契合免费额度上限。
- **优雅降级**：某次刷新时若限流/断网，个别币种短暂显示"暂不可用"，等下一个 30 分钟窗口自动重试；期间**不会**退回写死的假汇率（已按设计取消兜底）。
- **单进程缓存**：缓存为进程内内存（部署为 gunicorn 单 worker，缓存全局共享）；服务重启后首个查询会触发一次拉取。

## 五、验证

- 全模块 `py_compile` + `import` 通过；纯逻辑自测 `test_logic.py` 通过。
- 缓存节流单测：首次拉取 9 个币种（9 次调用）、30 分钟内再次查询 0 次调用命中缓存、买卖方向取价正确（买入取卖出价、卖出取买入价）。
- 线上：`/api/health` 返回 `{"ok":true}`，前端已切换到含 9 种币种的新版本。

---

## 六、同轮修订：行情主源改为「一次拉全部币种」

**问题**：以 Alpha Vantage 为源时，取 9 种货币需逐个发 9 次请求，而其免费额度为约 25 次/天、突发连打易只成功前几个；叠加“部分成功缓存 30 分钟不重试”，线上表现为**只有美元能查询、其余币种不可用**。

**修订**：
- **主源改为 open.er-api.com**：一次 HTTP 请求即返回全部币种对 CNY 的汇率（免密钥、无逐币种限流），“要么全有要么全无”，从根本上消除“只剩美元”。汇率与 Alpha Vantage 基本一致。
- **Alpha Vantage 降为兜底**：仅当主源故障或缺某币种时，用内置 Key 逐个补齐（通常不触发；Key 仍保留在 `config.py`）。
- **缓存策略修正**：集齐全部币种后缓存 30 分钟；若某次未集齐（主源临时故障），**60 秒即可重试**直至集齐，不再把不完整结果锁死 30 分钟。
- 涉及函数：`forex._fetch_all_mids_erapi()`（新主源）、`forex.refresh_rates()`（主源+兜底+智能重试间隔）；`_fetch_mid_raw()`（AV）保留为兜底。302/303/306/307 仍共用同一 30 分钟缓存，逻辑不变。

**验证**：真实调用 er-api 一次拉齐 9/9 币种、未触发 AV 兜底、缓存命中、买卖方向取价正确；全模块导入与逻辑自测通过。
